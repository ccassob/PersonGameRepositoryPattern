@model WebClient.Models.PersonViewModel

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>PersonDto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PersonDto.Name" class="control-label"></label>
                <input asp-for="PersonDto.Name" class="form-control" />
                <span asp-validation-for="PersonDto.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PersonDto.GameId" class="control-label"></label>
                <input asp-for="PersonDto.GameId" class="form-control" />
                <span asp-validation-for="PersonDto.GameId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
